# v0.2 Coding Tasks

每项任务都应在一个固定、可测试的目标仓库中准备独立起始提交。验收以目标仓库现有测试和任务特定断言为准；不要把 DSH Quality 自己的测试当作被测任务。

## 简单修改（01–05）

| ID | 任务 | 验收重点 |
| --- | --- | --- |
| 01 | 为字符串格式化工具增加 `trimAndCollapseWhitespace` 导出函数。 | 新增单元测试通过；原 API 不变。 |
| 02 | 为 HTTP 错误响应加入可选 `requestId` 字段。 | 序列化兼容；已有错误映射测试通过。 |
| 03 | 新增配置默认值 `retryDelayMs: 250`。 | 未配置时使用默认值；显式配置覆盖默认值。 |
| 04 | 在公共入口导出已存在的日期工具。 | package 入口导入成功；无循环依赖。 |
| 05 | 为 CLI 增加 `--quiet` 参数。 | 正常命令仍输出结果；quiet 时只保留退出码语义。 |

## Bug Fix（06–10）

| ID | 任务 | 初始故障与验收重点 |
| --- | --- | --- |
| 06 | 修复 UTC 日期在本地时区跨日时格式错误。 | 给定失败测试；边界日期通过。 |
| 07 | 修复空分页结果访问第一项导致的异常。 | 空数组返回空响应，不抛错。 |
| 08 | 修复 token 过期时间单位混用（秒/毫秒）。 | 过期与未过期 token 的测试都通过。 |
| 09 | 修复更新用户资料后缓存未失效。 | 更新后读取返回新值；回归测试通过。 |
| 10 | 修复重试次数的 off-by-one。 | `maxRetries=0/1/3` 都有精确断言。 |

## 边界条件（11–15）

| ID | 任务 | 验收重点 |
| --- | --- | --- |
| 11 | 为金额转换处理 `0`、负数和 `Number.MAX_SAFE_INTEGER`。 | 不溢出；非法值有明确错误。 |
| 12 | 为用户名长度校验加入 Unicode 代理对场景。 | 按 code point 而非 UTF-16 长度计数。 |
| 13 | 修复并发提交导致库存变为负数的边界。 | 可复现的并发测试通过。 |
| 14 | 读取可选配置文件不存在时应使用默认值。 | 文件缺失不报错；无权限错误仍上抛。 |
| 15 | 网络请求超时后应取消后续重试。 | 超时不泄漏定时器；取消测试通过。 |

## 需要多轮修复（16–20）

| ID | 任务 | 设计目的 |
| --- | --- | --- |
| 16 | 修复注册接口返回码错误，同时保持重复邮箱错误码不变。 | 第一次直觉修复容易破坏另一分支，验证应引导二次修复。 |
| 17 | 实现 CSV 导入的 quoted comma 支持，并保留空列。 | 需要处理解析、边界和回归三类失败。 |
| 18 | 修复异步队列 drain 时偶发丢任务。 | 先暴露时序失败，再修复并发状态。 |
| 19 | 修复权限中间件的角色继承，同时阻止循环继承。 | 第一次修复可能通过普通用例但失败于循环图。 |
| 20 | 修复测试命令生成临时文件后导致工作区状态改变的问题。 | 验证前后 fingerprint 变化；DSH Quality 应自动重验一次后给出稳定结论。 |
